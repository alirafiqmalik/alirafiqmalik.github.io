{
  "name": "alihamzamalik-github-io",
  "private": true,
  "description": "Academic portfolio site (Jekyll) — front-end dependencies",
  "scripts": {
    "build:globe": "esbuild assets/js/globe-src.js --bundle --format=iife --outfile=assets/js/globe.js --minify",
    "build": "npm run build:globe"
  },
  "dependencies": {
    "cobe": "^2.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
